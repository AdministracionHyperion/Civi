"""Geocoding validation building blocks.

Shared validated-geocode import is scope-driven (Manizales, Santander, …).
Municipal bounding boxes live in ``bounds``; rollout config in ``geocode_scopes``.
"""
