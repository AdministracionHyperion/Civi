[project]
name = "civi-simit-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.32",
  "httpx>=0.27",
  "playwright==1.49.1",
  "pydantic-settings>=2.4",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
