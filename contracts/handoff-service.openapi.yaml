openapi: 3.1.0
info:
  title: Civi Handoff Service
  version: 0.1.0
paths:
  /health/live:
    get:
      responses:
        "200":
          description: service is alive
  /health/ready:
    get:
      responses:
        "200":
          description: service is ready
  /internal/status:
    get:
      summary: Internal service status
      security:
        - internalBearer: []
      responses:
        "200":
          description: internal status
  /internal/handoffs:
    post:
      summary: Create human handoff case
      security:
        - internalBearer: []
      responses:
        "200":
          description: handoff queued
components:
  securitySchemes:
    internalBearer:
      type: http
      scheme: bearer
